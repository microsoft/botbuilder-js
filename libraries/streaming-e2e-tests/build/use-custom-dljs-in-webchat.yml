steps:
  - task: DownloadPipelineArtifact@2
    inputs:
      artifact: ReactApp
      path: $(TargetProjectDir)/react-app

  # - task: DownloadPipelineArtifact@2
  #   inputs:
  #     artifact: testReactApp
  #     path: $(TargetProjectDir)/testReactApp

  # TODO: keep tgz in folder of something we won't deploy (like DLJS)
  - powershell: npm install $(DLJSDir)/botframework-directlinejs.tgz
    displayName: npm install botframework-directlinejs.tgz
    workingDirectory: $(TargetProjectDir)/react-app

